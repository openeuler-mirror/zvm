description: linux virtual machine space

compatible: "linux-vm"

properties:
    dtb_address:
      type: int
      description: |
        vm dt file base address.
      required: true

    dtb_size:
      type: int
      description: |
        vm dt file size.
      required: true
